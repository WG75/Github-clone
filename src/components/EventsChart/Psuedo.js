// @flow
import React from 'react';
import PusedoElement from '../PsuedoElemnt';


const Psuedo = () => (
  <section className="received-events">
      <PusedoElement className="received-events_title received-events_title--is-psuedo" borderRadius={0} width={150} height={30} />
      <PusedoElement borderRadius={50} width={300} height={300} />
  </section>
);

export default Psuedo;
